import { StudentDataSearchPipe } from './student-data-search.pipe';

describe('StudentDataSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentDataSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
